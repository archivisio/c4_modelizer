import { createTheme } from '@mui/material/styles';

// Création d'un thème MUI par défaut
const theme = createTheme({
  // La configuration du thème sera ajoutée ici ultérieurement
});

export default theme;
